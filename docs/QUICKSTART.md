# Quick Start

```powershell
cd frontend
npm install
npm run dev
```

Use the local Vite URL. Validate the frontend with `npm run lint` and `npm run build`. The backend is not runnable in the current revision. See [Getting Started](Detailed_docs/GETTING_STARTED.md) for the full setup notes.
