# Versioning

Status: No explicit versioning strategy found. Recommend Semantic Versioning (`MAJOR.MINOR.PATCH`).
# Versioning

## Current strategy

The frontend package declares:

```text
1.0.0
```

in `frontend/package.json`.

No release tags or formal semantic-versioning workflow were established from the inspected repository metadata.

## Recommended policy

If the project adopts Semantic Versioning:

```text
MAJOR.MINOR.PATCH
```

- MAJOR: incompatible changes.
- MINOR: backward-compatible functionality.
- PATCH: backward-compatible fixes.

This is a recommendation until the team formally adopts it.
