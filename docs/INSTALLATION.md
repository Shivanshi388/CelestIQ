# Installation

The implemented application is installed from `frontend/` with Node.js and npm:

```powershell
cd frontend
npm install
```

No Python or database installation is currently required for the frontend demonstration. See [Dependencies](Detailed_docs/DEPENDENCIES.md) and [Configuration](Detailed_docs/CONFIGURATION.md) for verified toolchain details.
