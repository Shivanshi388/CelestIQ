# API Endpoints

**No live endpoints are available.** The proposed endpoint surface is maintained in the [API Contract](API_CONTRACT.md). The backend route files and frontend API adapters are reserved integration points, not evidence of a running service.
