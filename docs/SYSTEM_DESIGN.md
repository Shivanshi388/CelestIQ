# System Design

The current system is a React/Vite single-page application with Zustand state, mock services, browser localStorage, and a React Three Fiber visualization. The reserved backend pipeline is documented as target architecture only.

See [Architecture](ARCHITECTURE.md), [Data Flow](Detailed_docs/DATA_FLOW.md), and [Components](Detailed_docs/COMPONENTS.md).
